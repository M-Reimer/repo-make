# Build configuration for repo-make. If this file is managed by version control
# then you may place your own settings into a "repo-make-custom.conf" file
# which overloads settings of this config file.
# See repo-make.conf(5) for further information.

# Name of this repository
REPONAME=testrepo

# Optional script to check for common mistakes in newly built packages
#CHECKSCRIPT=./checkpkg

# Order to build packages. To temporarily disable build for a package, just
# comment the line by prefixing it with "#".
firstpackage
secondpackage
dir1/thirdpackage
